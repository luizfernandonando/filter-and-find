<div class="container">
  <h1 class="skills-heading"> Companies</h1>
    <div>
      <% @companies.each do |company| %>
  <hr>
    <%= link_to company.name, company_path(company) %>
    <%end %>
    </div>
</div>

